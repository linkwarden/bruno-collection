meta {
  name: Create Collection
  type: http
  seq: 1
}

post {
  url: {{host}}/api/v1/collections
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "New Collection",
    "description": "Collection description..."
  }
}
